/**
 * @author Aaron G
 * @discord meow2980
 * @version 0.1           (first one, I expect updates)
 * @date 5/10/25
 * @build 99.4            (latest build of DV since last update)
 */
public class Flatcar extends WorkTrains //Utility Short Flatcar
{
    private final static double MASS=3.65;//tons (empty)
    private final static int LENGTH_MM=9100;//milimeters (not used)
    private final static double LENGTH=9.1;//meters
    /**
     * Constructor for objects of class Flatcar
     */
    public Flatcar()
    {
        super(MASS,LENGTH);
    }
}
