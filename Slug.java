/**
 * @author Aaron G
 * @discord meow2980
 * @version 0.1           (first one, I expect updates)
 * @date 5/10/25
 * @build 99.4            (latest build of DV since last update)
 */
public class Slug extends WorkTrains//DE6-860S
{
    private final static int MASS=125;//tons (empty)
    private final static int LENGTH_MM=16800;//milimeters (not used)
    private final static double LENGTH=16.8;//meters
    /**
     * Constructor for objects of class Slug
     */
    public Slug()
    {
        super(MASS,LENGTH);
    }
}
